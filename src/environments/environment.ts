// This file can be replaced during build by using the `fileReplacements` array.
// `ng build ---prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  transcriptionType: 'local',
  debug: false,
  transcriptionAPI: 'https://api.andaluh.es/epa',
  casPlaceholder: '"Toda comunidad lingüística tiene derecho a codificar, estandarizar, preservar, desarrollar y promover su sistema lingüístico, sin interferencias inducidas o forzadas." Art. 9 de la Declaración Universal de los Derechos Lingüísticos.',
  andPlaceholder: '"Toa comunidá linguíttica tiene derexo a codificâh, êttandariçâh, preçerbâh, deçarroyâh y promobêh çu çîttema linguíttico, çin interferençiâ induçidâ o forçâh." Art. 9 de la Declaraçión Uniberçâh de lô Derexô Linguítticô.'
};

/*
 * In development mode, to ignore zone related error stack frames such as
 * `zone.run`, `zoneDelegate.invokeTask` for easier debugging, you can
 * import the following file, but please comment it out in production mode
 * because it will have performance impact when throw error
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
